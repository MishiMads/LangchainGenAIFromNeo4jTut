[pytest]
# Configure pytest for cleaner output
addopts = --tb=short --no-header -q
# --tb=short: Short traceback format (less verbose)
# --no-header: Don't show pytest version info
# -q: Quiet mode (less output)

